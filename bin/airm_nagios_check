#!/usr/bin/env ruby

lib = File.expand_path('../lib', __FILE__)
$:.unshift(lib) unless $:.include?(lib)

require 'airm_nagios_check'


AirmNagiosCheck::Base.new(:filename=>Choice.choices[:file]).check()